syntax = "proto3";

package data.storage;

option go_package = "github.com/bcrusu/scout/internal/data/server/storage";

import "internal/data/server/txn/txn.proto";

message Command {
    oneof payload {
        Barrier barrier = 1;
        txn.Batch batch = 2;
    }
}

message Snapshot {
    uint64 index = 1;
    txn.Snapshot txn_snapshot = 2;
}

message Barrier {}
